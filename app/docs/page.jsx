import DocsClient from "@/components/DocsClient";

export const metadata = {
  title: "Docs — Oasis",
  description:
    "Everything you need to understand Oasis, Genesis pools, locked drops, wallet tracking, and Robinhood Chain launch mechanics.",
};

export default function DocsPage() {
  return <DocsClient />;
}
